// TODO: Do not enable to define client side param / qualifiers for non-concrete (interface) types, so
//      configuration.register<Interface>(Object).instanceOf().param(Object, "method", "p", "fucked") is invalid
